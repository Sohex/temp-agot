propose_trade_enclave_war_decision = {
    picture = {
        reference = "gfx/interface/illustrations/decisions/dragonlord_council.png"
    }
    decision_group_type = minor

    desc = propose_trade_enclave_war_decision_desc
    selection_tooltip = propose_trade_enclave_war_decision_tooltip
    confirm_text = propose_trade_enclave_war_decision_confirm

    is_shown = {
        is_ai = no
        top_liege = this
        government_has_flag = government_is_dragonlord_oligarchy
    }

    is_valid = {
        is_at_war = no
        NOT = { has_character_flag = dragonlord_council_meeting_cooldown }
        NOT = { has_variable = dragonlord_council_pending_agenda }
        NOT = { has_variable = dragonlord_council_agenda_window_open }
    }

    effect = {
        hidden_effect = {
            clear_variable_list = dragonlord_council_target_candidates
            clear_variable_list = dragonlord_council_forecast_supporters
            clear_variable_list = dragonlord_council_forecast_opponents

            remove_variable = dragonlord_council_selected_law
            remove_variable = dragonlord_council_selected_target
            remove_variable = dragonlord_council_selected_development_type
            remove_variable = dragonlord_council_selected_development_target
            remove_variable = dragonlord_council_selected_estate_target
            remove_variable = dragonlord_council_vote_forecast_open

            # The war confirmation flow snapshots these values even though war
            # motions do not use the finance controls.
            set_variable = { name = dragonlord_council_budget_salary value = 0 }
            set_variable = { name = dragonlord_council_budget_military value = 0 }
            set_variable = { name = dragonlord_council_budget_salary_original value = 0 }
            set_variable = { name = dragonlord_council_budget_military_original value = 0 }

            set_variable = {
                name = dragonlord_council_selected_agenda_category
                value = flag:dragonlord_war_agenda
            }
            set_variable = {
                name = dragonlord_council_selected_war_type
                value = flag:dragonlord_trade_enclave_war_agenda
            }
            dragonlord_council_rebuild_war_target_pool_effect = yes

            set_variable = {
                name = dragonlord_council_agenda_window_open
                value = yes
            }
        }
    }

    ai_potential = { always = no }
    ai_will_do = { base = 0 }
}
