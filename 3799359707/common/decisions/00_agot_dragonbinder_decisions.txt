### Search for a Dragonbinder Horn - universal, no gate at all, rarer than
### the glass candle (this is the mod's single most powerful tool).
agot_dragonbinder_search_decision = {
	ai_check_interval = 24

	picture = {
		reference = "gfx/interface/illustrations/decisions/agot_decision_dragonbinder.dds"
	}

	desc = agot_dragonbinder_search_decision_desc
	selection_tooltip = agot_dragonbinder_search_decision_tooltip

	cooldown = { years = 5 }

	is_shown = {
		NOT = { any_character_artifact = { artifact_type = dragon_horn } }
	}

	is_valid_showing_failures_only = {
		is_adult = yes
	}

	is_valid = {
		is_adult = yes
	}

	effect = {
		stress_impact = { base = minor_stress_impact_gain }
		random_list = {
			4 = {
				agot_dragonbinder_create_horn_effect = yes
			}
			96 = {
				send_interface_toast = {
					type = event_toast_effect_bad
					title = agot_dragonbinder_search_failed_toast
					left_icon = root
				}
			}
		}
	}

	ai_potential = {
		is_adult = yes
	}

	ai_will_do = {
		base = 5 # even the AI is wary of this one
	}
}
