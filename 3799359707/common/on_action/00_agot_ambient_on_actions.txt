# AGOT: Magic of Westeros - Ambient events (see V3_FIXPLAN.md Block 3)
# Hooks the vanilla per-character yearly pulse. Only the events={} list
# is touched here - never trigger/random_on_action, which vanilla
# already defines for this on_action and which would NOT merge safely
# if redefined (see feedback_ck3_debugging_approach memory).

random_yearly_playable_pulse = {
	events = {
		agot_ambient.0001
	}
}
