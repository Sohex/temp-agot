# AGOT: Magic of Westeros v2 - Drowned God
# A permanent, strong hook representing the near-drowning "binding" rite -
# reuses CK3's real hook system so a bound character is actually blocked
# from joining factions against their binder (see passes_faction_hard_block
# in the base game's common/scripted_rules/00_rules.txt, which already
# checks has_strong_hook for exactly this purpose). No new faction files
# need to be touched.

agot_drowned_binding_hook = {
	strong = yes
}
