macro = {
	description = "Returns the int32 as a string."
	definition = "GetString_int32(Arg0)"
	replace_with = "GuiScope.AddScope( 'number', MakeScopeValue(IntToFixedPoint(Arg0))).Custom('NumberToString')"
}

macro = {
	description = "Returns the CFixedPoint as a string."
	definition = "GetString_CFixedPoint(Arg0)"
	replace_with = "GuiScope.AddScope( 'number', MakeScopeValue(Arg0)).Custom('NumberToString')"
}