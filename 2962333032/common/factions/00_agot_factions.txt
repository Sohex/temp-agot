agot_loyalist_faction = {
	casus_belli = liberty_faction_war
	short_effect_desc = agot_loyalist_faction_short_effect_desc
	sort_order = 0
	discontent_progress = {
		base = 0
	}
	power_threshold = {
		base = 100000
	}
	is_valid = {}
	is_character_valid = {
		agot_loyalist_faction_character_validity_trigger = {
			FACTION_TARGET = scope:faction.faction_target
		}
	}
	demand = {
	}
	can_character_create_ui = {
		agot_loyalist_faction_can_character_create_trigger = {
			FACTION_TARGET = scope:target
		}
		#NOT = { has_character_flag = recently_disbanded_loyalist_faction }
		liege = {
			NOT = { has_government = theocracy_government }
			NOT = { government_has_flag = government_is_nw }
			#is_at_war = no
		}

		custom_description = {
			text = character_blocked_from_joining
			NOT = {
				has_character_flag = joining_faction_block
			}
		}
	}
	can_character_create = {
		agot_loyalist_faction_can_character_create_trigger = {
			FACTION_TARGET = scope:target
		}
		#NOT = { has_character_flag = recently_disbanded_loyalist_faction }
		liege = {
			NOT = { has_government = theocracy_government }
			NOT = { government_has_flag = government_is_nw }
			#is_at_war = no
		}
	}

	ai_create_score = {
		base = 0
		agot_loyalist_faction_modifiers = {
			FACTION_TARGET = scope:target
			OPINION_MULTIPLIER = 10
			MAX_OPINION = 10000
		}
	}
	can_character_join = {
		agot_loyalist_faction_can_character_join_trigger = {
			FACTION_TARGET = scope:faction.faction_target
		}
	}

	ai_join_score = {
		base = -350
		agot_loyalist_faction_modifiers = {
			FACTION_TARGET = scope:faction.faction_target
			OPINION_MULTIPLIER = 7
			MAX_OPINION = 10000
		}
	}
	ai_demand_chance = {
		base = -10000

		modifier = {
			factor = 0
			faction_leader = {
				is_ai = yes
			}
		}
	}
	county_join_score = {
		base = 0
	}
	is_county_valid = {
	}

	can_character_become_leader = {
		is_landed = yes
	}

	character_leaves = {
		scope:faction_member = {
			if = {
				limit = {
					is_alive = yes
				}
				add_character_flag = {
					flag = recently_disbanded_loyalist_faction
					years = 1
				}
			}
		}
	}

	requires_leader = no
	leaders_allowed_to_leave = yes
	county_allow_join = no
	county_allow_create = no
	player_can_join = yes
}