lv_agot_ruin_can_pay_trigger = {
	trigger_if = {
		limit = { scope:actor = { has_treasury = yes } }
		scope:actor = { treasury >= $AMOUNT$ }
	}
	trigger_else = {
		scope:actor = { gold >= $AMOUNT$ }
	}
}
