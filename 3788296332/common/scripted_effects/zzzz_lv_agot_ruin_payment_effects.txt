lv_agot_ruin_pay_effect = {
	if = {
		limit = {
			scope:actor = {
				has_treasury = yes
				treasury >= $AMOUNT$
			}
		}
		scope:actor = { remove_short_term_treasury = $AMOUNT$ }
	}
	else = {
		scope:actor = { remove_short_term_gold = $AMOUNT$ }
	}
}
