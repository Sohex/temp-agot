namespace = dragonlord_colonization

dragonlord_colonization.0001 = {
	type = character_event
	title = dragonlord_colonization.0001.t
	desc = dragonlord_colonization.0001.desc
	theme = realm
	left_portrait = { character = root animation = personality_rational }
	right_portrait = { character = scope:dragonlord_colonial_governor animation = personality_rational }
	trigger = {
		is_alive = yes
		exists = scope:dragonlord_colonial_city
		scope:dragonlord_colonial_city = {
			has_variable = dragonlord_colonial_free_city
			holder = { is_alive = yes is_landed = yes liege = root }
		}
	}
	immediate = {
		# An appointment can occur between victory and the event's next tick.
		scope:dragonlord_colonial_city.holder = { save_scope_as = dragonlord_colonial_governor }
	}
	option = {
		name = dragonlord_colonization.0001.a
		custom_tooltip = dragonlord_colonization.0001.a.tt
		ai_chance = { base = 70 }
	}
	option = {
		name = dragonlord_colonization.0001.b
		custom_tooltip = dragonlord_colonization.0001.b.tt
		hidden_effect = {
			if = {
				limit = {
					scope:dragonlord_colonial_city.holder = {
						is_alive = yes
						is_landed = yes
						liege = root
					}
				}
				scope:dragonlord_colonial_city.holder = { save_scope_as = dragonlord_colonial_governor }
				dragonlord_colonization_replace_governor_effect = yes
			}
		}
		ai_chance = { base = 30 }
	}
}
