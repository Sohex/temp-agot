afs_resume_decision = {
    picture = { reference = "gfx/interface/illustrations/decisions/decision_realm.dds" }
    desc = afs_resume_decision_desc
    selection_tooltip = afs_resume_decision_tooltip
    confirm_text = afs_resume_decision_confirm
    ai_check_interval = 0
    is_shown = {
        is_ai = no
        has_character_flag = afs_pending
        
    }
    is_valid = {
        custom_tooltip = {
            text = afs.available_site_tt
            afs_has_eligible_site = yes
        }
    }
    effect = { afs_open_selection_effect = yes }
}

afs_additional_seat_decision = {
 picture = { reference = "gfx/interface/illustrations/decisions/decision_realm.dds" }
 desc = afs_additional_seat_decision_desc
 selection_tooltip = afs_additional_seat_decision_tooltip
 confirm_text = afs_additional_seat_decision_confirm
 ai_check_interval = 0
 is_shown = { is_ai = no is_landed = yes }
 is_valid = {
  gold >= 6000
  prestige >= 1000
  custom_tooltip = { text = afs.no_active_choice_tt NOR = { has_character_flag = afs_pending has_character_flag = afs_scheduled has_trait = afs_founders_ambition } }
  custom_tooltip = { text = afs.available_site_tt afs_has_eligible_site = yes }
 }
 cost = { gold = 6000 prestige = 1000 }
 effect = { afs_paid_seat_effect = yes }
}

afs_cancel_decision = {
 picture = { reference = "gfx/interface/illustrations/decisions/decision_realm.dds" }
 desc = afs_cancel_decision_desc
 selection_tooltip = afs_cancel_decision_tooltip
 confirm_text = afs_cancel_decision_confirm
 ai_check_interval = 0
 is_shown = { is_ai = no OR = { has_trait = afs_founders_ambition has_character_flag = afs_pending has_character_flag = afs_scheduled has_character_flag = afs_paid_receipt } }
 effect = { afs_cancel_effect = yes }
}
