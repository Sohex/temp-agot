afs_venue_score_agot_sailing = {
 value = 0
 if = { limit = { has_building = afs_tidegate_harbour_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_silent_harbour_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_aleport_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_chainwater_arsenal_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_saltchain_hall_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_lantern_quay_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_saltglass_harbour_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_saltwake_landing_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_lockwarden_quay_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_sailmakers_close_01 agot_generic_is_special_building_enabled = yes } add = 20 }
}
afs_venue_score_funeral = {
 value = 0
 if = { limit = { has_building = afs_barrowflame_circle_01 agot_generic_is_special_building_enabled = yes } add = 20 }
 if = { limit = { has_building = afs_weeping_bough_enclosure_01 agot_generic_is_special_building_enabled = yes } add = 10 }
 if = { limit = { has_building = afs_gentle_hand_temple_01 agot_generic_is_special_building_enabled = yes } add = 10 }
 if = { limit = { has_building = afs_fossil_crown_priory_01 agot_generic_is_special_building_enabled = yes } add = 5 }
}
afs_venue_score_wedding = {
 value = 0
 if = { limit = { has_building = afs_pearlwater_pavilion_01 agot_generic_is_special_building_enabled = yes } add = 50 }
 if = { limit = { has_building = afs_summerheart_manor_01 agot_generic_is_special_building_enabled = yes } add = 25 }
 if = { limit = { has_building = afs_kings_rest_01 agot_generic_is_special_building_enabled = yes } add = 25 }
 if = { limit = { has_building = afs_rosehip_castle_01 agot_generic_is_special_building_enabled = yes } add = 25 }
 if = { limit = { has_building = afs_andal_palace_01 agot_generic_is_special_building_enabled = yes } add = 25 }
 if = { limit = { has_building = afs_palace_of_the_arts_01 agot_generic_is_special_building_enabled = yes } add = 25 }
}
afs_venue_score_adult_education = {
 value = 0
 if = { limit = { has_building = afs_sages_cloister_01 agot_generic_is_special_building_enabled = yes } add = 10 }
 if = { limit = { has_building = afs_lantern_library_01 agot_generic_is_special_building_enabled = yes } add = 5 }
 if = { limit = { has_building = afs_emberglass_archive_01 agot_generic_is_special_building_enabled = yes } add = 5 }
 if = { limit = { has_building = afs_starwatch_observatory_01 agot_generic_is_special_building_enabled = yes } add = 5 }
}
afs_venue_score_roaming = {
 value = 0
 if = { limit = { has_building = afs_yewguard_lodge_01 agot_generic_is_special_building_enabled = yes } add = 5 }
 if = { limit = { has_building = afs_hartwatch_hold_01 agot_generic_is_special_building_enabled = yes } add = 5 }
 if = { limit = { has_building = afs_wolfpelt_lodge_01 agot_generic_is_special_building_enabled = yes } add = 5 }
 if = { limit = { has_building = afs_cloudbreak_citadel_01 agot_generic_is_special_building_enabled = yes } add = 5 }
}
afs_venue_score_coronation = {
 value = 0
 if = { limit = { has_building = afs_andal_palace_01 agot_generic_is_special_building_enabled = yes } add = 5 }
 if = { limit = { has_building = afs_heralds_court_01 agot_generic_is_special_building_enabled = yes } add = 5 }
}
afs_venue_score_agot_coronation = {
 value = 0
 if = { limit = { has_building = afs_andal_palace_01 agot_generic_is_special_building_enabled = yes } add = 5 }
 if = { limit = { has_building = afs_heralds_court_01 agot_generic_is_special_building_enabled = yes } add = 5 }
}
afs_venue_score_tournament = {
 value = 0
 if = { limit = { has_building = afs_rosehip_castle_01 agot_generic_is_special_building_enabled = yes } add = 50 }
 if = { limit = { has_building = afs_dawnspire_01 agot_generic_is_special_building_enabled = yes } add = 25 }
}
afs_venue_score_hunt = {
 value = 0
 if = { limit = { has_building = afs_hartwatch_hold_01 agot_generic_is_special_building_enabled = yes } add = 100 }
 if = { limit = { has_building = afs_wolfpelt_lodge_01 agot_generic_is_special_building_enabled = yes } add = 50 }
}
afs_venue_score_feast = {
 value = 0
 if = { limit = { has_building = afs_kings_rest_01 agot_generic_is_special_building_enabled = yes } add = 50 }
 if = { limit = { has_building = afs_aleport_01 agot_generic_is_special_building_enabled = yes } add = 50 }
 if = { limit = { has_building = afs_merrymantle_castle_01 agot_generic_is_special_building_enabled = yes } add = 100 }
 if = { limit = { has_building = afs_goldenfurrow_estate_01 agot_generic_is_special_building_enabled = yes } add = 50 }
 if = { limit = { has_building = afs_singers_crown_01 agot_generic_is_special_building_enabled = yes } add = 50 }
 if = { limit = { has_building = afs_palace_of_the_arts_01 agot_generic_is_special_building_enabled = yes } add = 50 }
 if = { limit = { has_building = afs_stagdrum_hall_01 agot_generic_is_special_building_enabled = yes } add = 25 }
 if = { limit = { has_building = afs_oathfire_moot_01 agot_generic_is_special_building_enabled = yes } add = 25 }
}
