namespace = lv_colonisation_events

lv_colonisation_events.0001 = {
	type = character_event
	title = lv_colonisation_events.0001.t
	desc = lv_colonisation_events.0001.desc
	theme = travel

	# Remove once implemented pls
	orphan = yes

	left_portrait = {
		character = root
		animation = personality_honorable
	}

	# A: Outsmart Robin
	option = {
		name = lv_colonisation_events.0001.a
		custom_tooltip = lv_colonisation_events.0001.a.tip
		trigger = { learning > 20 }
        random_list = {
            60 = { add_gold = 40 add_prestige = 40 }
            # 40 = { add_stress = 25 pay_short_term_gold = { target = (Robin scope) gold = 20 } }
        }
	}

	# B: Logistics play
	option = {
		name = lv_colonisation_events.0001.b
		custom_tooltip = lv_colonisation_events.0001.b.tip
		trigger = { stewardship > 20 }
        random_list = {
            65 = { add_prestige = 30 add_gold = 20 }
            # 35 = { add_stress = 20 pay_short_term_gold = { target = (Robin scope) gold = 30 } }
		}
	}

	# C: Recruit John
	option = {
		name = lv_colonisation_events.0001.c
		custom_tooltip = lv_colonisation_events.0001.c.tip
		trigger = { diplomacy > 20 }
        random_list = {
            55 = {
                create_character = {
                    age = { 16 55 }
                    gender = male
                    random_traits = yes
                    name = "John"
                    save_scope_as = john_the_defector
                    culture = location.culture
                    faith = location.faith
                    location = scope:location

                    trait = ambitious
                    trait = intellect_good_1
                    trait = strategist
                    trait = education_martial_3
                    trait = education_martial_prowess_2
                }
                add_courtier = scope:john_the_defector
                add_prestige = 80
            }
            45 = { add_stress = 30 }
		}
	}

	# D: Martial response
	option = {
		name = lv_colonisation_events.0001.d
		custom_tooltip = lv_colonisation_events.0001.d.tip
		trigger = { martial > 20 }
        random_list = {
            60 = { add_prestige = 50 add_gold = 25 }
            40 = { add_stress = 25 } # Wound
        }
	}

	# E: Fear and shadow
	option = {
		name = lv_colonisation_events.0001.e
		custom_tooltip = lv_colonisation_events.0001.e.tip
		trigger = { intrigue > 20 }
        random_list = {
            60 = { add_prestige = 40 add_gold = 30 }
            40 = { add_stress = 20 }
		}
	}

	# F: Duel Robin
	option = {
		name = lv_colonisation_events.0001.f
		custom_tooltip = lv_colonisation_events.0001.f.tip
		trigger = { prowess > 20 }
        random_list = {
            50 = { add_prestige = 100 add_trait = brave }
            50 = { add_trait = wounded_1 add_stress = 35 }
		}
	}

	# Z: Bribe and hope
	option = {
		name = lv_colonisation_events.0001.z
		custom_tooltip = lv_colonisation_events.0001.z.tip
        random_list = {
            25 = { add_gold = 100 add_prestige = 50 }
            25 = { add_gold = 50 }
            # 25 = { pay_short_term_gold = { target = (Robin scope) gold = 25 } }
            # 25 = { pay_short_term_gold = { target = (Robin scope) gold = 100 } add_stress = 25 }
		}
	}
}
