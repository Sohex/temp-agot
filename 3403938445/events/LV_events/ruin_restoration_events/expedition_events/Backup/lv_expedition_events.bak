namespace = lv_expedition_event

# Directory

# lv_expedition_event.0001 - Decide whether to lead the expedition yourself or assign an expedition leader
# lv_expedition_event.0010 - Arrival

# Decide whether to lead the expedition yourself or assign an expedition leader
lv_expedition_event.0001 = {
	type = character_event
	title = lv_expedition_event.0001.t
	desc = lv_expedition_event.0001.desc
	theme = travel

	# Remove once implemented pls
	orphan = yes

	right_portrait = {
		character = root
		animation = thinking
	}

	left_portrait = {
		character = scope:new_leader
		animation = thinking
	}

	immediate = {
		random_courtier = {
			save_scope_as = new_leader
		}
	}

	# Personally lead the expedition - Proceed to next phase
	option = {
		name = lv_expedition_event.0001.a
		current_travel_plan = { resume_travel_plan = yes }
	}

	# Choose a courtier to be the expedition leader - Set up story cycle for selected courtier then end activity, assign story cycle instead
	option = {
		name = lv_expedition_event.0001.b
		#skip_activity_phase = yes
		current_travel_plan = {
			travel_plan_activity = { set_activity_host = scope:new_leader }
		}
	}
}

# Select expedition leader

# Arrival
lv_expedition_event.0010 = {
	type = character_event
	title = {
		first_valid = {
			triggered_desc = {
				trigger = { location = { geographical_region = world_valyria_proper } }
				desc = lv_expedition_event.0010.t.valyria
			}
			triggered_desc = {
				trigger = { location = { geographical_region = world_valyria_lands_of_the_long_summer } }
				desc = lv_expedition_event.0010.t.lands_of_the_long_summer
			}
		}
	}
	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { location = { geographical_region = world_valyria_proper } }
				desc = lv_expedition_event.0010.desc.valyria
			}
			triggered_desc = {
				trigger = { location = { geographical_region = world_valyria_lands_of_the_long_summer } }
				desc = lv_expedition_event.0010.desc.lands_of_the_long_summer
			}
		}
	}
	theme = travel

	# Remove once implemented pls
	orphan = yes

	left_portrait = {
		character = root
		animation = survey
	}

	# Valyria
	override_background = {
		trigger = {
			location = {
				OR = {
					geographical_region = world_valyria_proper
					geographical_region = world_valyria_lands_of_the_long_summer
				}
				OR = {
					agot_is_urban_terrain = yes
					has_building_or_higher = large_ruined_holding_01
				}
			}
		}
		reference = lv_valyria_large
	}
	override_background = {
		trigger = {
			location = {
				OR = {
					geographical_region = world_valyria_proper
					geographical_region = world_valyria_lands_of_the_long_summer
				}
				has_building_or_higher = medium_ruined_holding_01
			}
		}
		reference = lv_valyria_medium
	}
	override_background = {
		trigger = {
			location = {
				OR = {
					geographical_region = world_valyria_proper
					geographical_region = world_valyria_lands_of_the_long_summer
				}
			}
		}
		reference = lv_valyria_small
	}

	override_effect_2d = {
		trigger = {
			location = {
				OR = {
					geographical_region = world_valyria_proper
					geographical_region = world_valyria_lands_of_the_long_summer
				}
			}
		}
		reference = smoke
	}

	option = {
		name = {
			trigger = { location = { geographical_region = world_valyria_proper } }
			text = lv_expedition_event.0010.a.valyria
		}
		name = {
			trigger = { location = { geographical_region = world_valyria_lands_of_the_long_summer } }
			text = lv_expedition_event.0010.a.lands_of_the_long_summer
		}
		custom_tooltip = lv_expedition_event.0010.a.tt
	}
}
