# Scripted Triggers and Effects
scripted_trigger lv_has_blocker_swamp = {
	OR = {
		has_building_or_higher = swamp_01_03
		has_building_or_higher = swamp_02_03
	}
}

scripted_trigger lv_has_blocker_noxious_gases = {
	OR = {
		has_building_or_higher = noxious_gases_01_03
		has_building_or_higher = noxious_gases_02_03
	}
}

namespace = lv_blocker_clearance

# Directory
# lv_blocker_clearance.0001 - Successful clearance

# lv_blocker_clearance.0011 - Swamp: Challenge event
# lv_blocker_clearance.0012 - Swamp: Failure
# lv_blocker_clearance.0013 - Swamp: Critical Failure (Hard Reset)
# lv_blocker_clearance.0014 - Swamp: Success
# lv_blocker_clearance.0015 - Swamp: Critical Success (Guaranteed Reward)

# lv_blocker_clearance.0021 - Floods: Challenge event
# lv_blocker_clearance.0022 - Floods: Failure
# lv_blocker_clearance.0023 - Floods: Critical Failure (Hard Reset)
# lv_blocker_clearance.0024 - Floods: Success
# lv_blocker_clearance.0025 - Floods: Critical Success (Guaranteed Reward)

# lv_blocker_clearance.0031 - Noxious Gases: Challenge event
# lv_blocker_clearance.0032 - Noxious Gases: Failure
# lv_blocker_clearance.0033 - Noxious Gases: Critical Failure (Hard Reset)
# lv_blocker_clearance.0034 - Noxious Gases: Success
# lv_blocker_clearance.0035 - Noxious Gases: Critical Success (Guaranteed Reward)

# lv_blocker_clearance.0041 - Stonemen Encampment: Challenge event
# lv_blocker_clearance.0042 - Stonemen Encampment: Failure
# lv_blocker_clearance.0043 - Stonemen Encampment: Critical Failure (Hard Reset)
# lv_blocker_clearance.0044 - Stonemen Encampment: Success
# lv_blocker_clearance.0045 - Stonemen Encampment: Critical Success (Guaranteed Reward)

# lv_blocker_clearance.0051 - Bandit Camps: Challenge event
# lv_blocker_clearance.0052 - Bandit Camps: Failure
# lv_blocker_clearance.0053 - Bandit Camps: Critical Failure (Hard Reset)
# lv_blocker_clearance.0054 - Bandit Camps: Success
# lv_blocker_clearance.0055 - Bandit Camps: Critical Success (Guaranteed Reward)

# Successful clearance
lv_blocker_clearance.0001 = {
	type = character_event
	title = {
		first_valid = {
			first_valid = {
				triggered_desc = {
					trigger = { 
						scope:blocker_province = {
							has_variable = blocker_outcome
							has_building_or_higher = unexplored_caves_03
						}
					}
					desc = lv_blocker_clearance.0001.t.unexplored_caves.reward
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							has_variable = blocker_outcome
							lv_has_blocker_swamp = yes
						}
					}
					desc = lv_blocker_clearance.0001.t.swamp.reward
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							has_variable = blocker_outcome
							has_building_or_higher = lv_floods_03
						}
					}
					desc = lv_blocker_clearance.0001.t.floods.reward
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							has_variable = blocker_outcome
							lv_has_blocker_noxious_gases = yes
						}
					}
					desc = lv_blocker_clearance.0001.t.noxious_gases.reward
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							has_variable = blocker_outcome
							has_building_or_higher = stonemen_encampment_03
						}
					}
					desc = lv_blocker_clearance.0001.t.stonemen.reward
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							has_variable = blocker_outcome
							has_building_or_higher = bandit_camps_03
						}
					}
					desc = lv_blocker_clearance.0001.t.bandit_camps.reward
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							has_variable = blocker_outcome
							has_building_or_higher = small_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.t.small_ruined_holding.reward
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							has_variable = blocker_outcome
							has_building_or_higher = medium_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.t.medium_ruined_holding.reward
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							has_variable = blocker_outcome
							has_building_or_higher = large_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.t.large_ruined_holding.reward
				}
				# Generic treasure title here
				# desc = lv_blocker_clearance.0001.t.generic.reward
			}
			first_valid = {
				triggered_desc = {
					trigger = { scope:blocker_province = { has_building_or_higher = unexplored_caves_03 } }
					desc = lv_blocker_clearance.0001.t.unexplored_caves
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { lv_has_blocker_swamp = yes } }
					desc = lv_blocker_clearance.0001.t.swamp
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { has_building_or_higher = lv_floods_03 } }
					desc = lv_blocker_clearance.0001.t.floods
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { lv_has_blocker_noxious_gases = yes } }
					desc = lv_blocker_clearance.0001.t.noxious_gases
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { has_building_or_higher = stonemen_encampment_03 } }
					desc = lv_blocker_clearance.0001.t.stonemen
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { has_building_or_higher = bandit_camps_03 } }
					desc = lv_blocker_clearance.0001.t.bandit_camps
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { has_building_or_higher = small_ruined_holding_03 } }
					desc = lv_blocker_clearance.0001.t.small_ruined_holding
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { has_building_or_higher = medium_ruined_holding_03 } }
					desc = lv_blocker_clearance.0001.t.medium_ruined_holding
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { has_building_or_higher = large_ruined_holding_03 } }
					desc = lv_blocker_clearance.0001.t.large_ruined_holding
				}
				# Generic title here
				# desc = lv_blocker_clearance.0001.t.generic
			}
		}
	}
	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { scope:blocker_province = { has_building_or_higher = unexplored_caves_03 } }
				desc = lv_blocker_clearance.0001.desc.unexplored_caves
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { lv_has_blocker_swamp = yes } }
				desc = lv_blocker_clearance.0001.desc.swamp
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { has_building_or_higher = lv_floods_03 } }
				desc = lv_blocker_clearance.0001.desc.floods
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { lv_has_blocker_noxious_gases = yes } }
				desc = lv_blocker_clearance.0001.desc.noxious_gases
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { has_building_or_higher = stonemen_encampment_03 } }
				desc = lv_blocker_clearance.0001.desc.stonemen
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { has_building_or_higher = bandit_camps_03 } }
				desc = lv_blocker_clearance.0001.desc.bandit_camps
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { has_building_or_higher = small_ruined_holding_03 } }
				desc = lv_blocker_clearance.0001.desc.small_ruined_holding
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { has_building_or_higher = medium_ruined_holding_03 } }
				desc = lv_blocker_clearance.0001.desc.medium_ruined_holding
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { has_building_or_higher = large_ruined_holding_03 } }
				desc = lv_blocker_clearance.0001.desc.large_ruined_holding
			}
			# Generic desc here
			# desc = lv_blocker_clearance.0001.desc.generic
		}
		first_valid = {
			triggered_desc = {
				trigger = {
					scope:blocker_province = {
						var:blocker_outcome ?= flag:dragon_egg
						has_building_or_higher = unexplored_caves_03
					}
				}
				desc = lv_blocker_clearance.0001.desc.unexplored_caves.dragon_egg
			}
			triggered_desc = {
				trigger = { scope:blocker_province = { lv_has_blocker_swamp = yes } }
				desc = lv_blocker_clearance.0001.desc.swamp.dragon_egg
			}
			triggered_desc = {
				trigger = {
					scope:blocker_province = {
						var:blocker_outcome ?= flag:dragon_egg
						has_building_or_higher = lv_floods_03
					}
				}
				desc = lv_blocker_clearance.0001.desc.floods.dragon_egg
			}
			triggered_desc = {
				trigger = {
					scope:blocker_province = {
						var:blocker_outcome ?= flag:dragon_egg
						lv_has_blocker_noxious_gases = yes
					}
				}
				desc = lv_blocker_clearance.0001.desc.noxious_gases.dragon_egg
			}
			triggered_desc = {
				trigger = {
					scope:blocker_province = {
						var:blocker_outcome ?= flag:dragon_egg
						has_building_or_higher = stonemen_encampment_03
					}
				}
				desc = lv_blocker_clearance.0001.desc.stonemen.dragon_egg
			}
			triggered_desc = {
				trigger = {
					scope:blocker_province = {
						var:blocker_outcome ?= flag:dragon_egg
						has_building_or_higher = bandit_camps_03
					}
				}
				desc = lv_blocker_clearance.0001.desc.bandit_camps.dragon_egg
			}
			triggered_desc = {
				trigger = {
					scope:blocker_province = {
						var:blocker_outcome ?= flag:dragon_egg
						has_building_or_higher = small_ruined_holding_03
					}
				}
				desc = lv_blocker_clearance.0001.desc.small_ruined_holding.dragon_egg
			}
			triggered_desc = {
				trigger = {
					scope:blocker_province = {
						var:blocker_outcome ?= flag:dragon_egg
						has_building_or_higher = medium_ruined_holding_03
					}
				}
				desc = lv_blocker_clearance.0001.desc.medium_ruined_holding.dragon_egg
			}
			triggered_desc = {
				trigger = {
					scope:blocker_province = {
						var:blocker_outcome ?= flag:dragon_egg
						has_building_or_higher = large_ruined_holding_03
					}
				}
				desc = lv_blocker_clearance.0001.desc.large_ruined_holding.dragon_egg
			}
			# Generic dragon egg desc here
			# desc = lv_blocker_clearance.0001.desc.generic.dragon_egg
		}
		first_valid = {
			first_valid = {
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_high
							has_building_or_higher = unexplored_caves_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.unexplored_caves.gold_high
				}
				triggered_desc = {
					trigger = { scope:blocker_province = { lv_has_blocker_swamp = yes } }
					desc = lv_blocker_clearance.0001.desc.swamp.gold_high
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_high
							has_building_or_higher = lv_floods_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.floods.gold_high
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_high
							lv_has_blocker_noxious_gases = yes
						}
					}
					desc = lv_blocker_clearance.0001.desc.noxious_gases.gold_high
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_high
							has_building_or_higher = stonemen_encampment_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.stonemen.gold_high
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_high
							has_building_or_higher = bandit_camps_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.bandit_camps.gold_high
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_high
							has_building_or_higher = small_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.small_ruined_holding.gold_high
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_high
							has_building_or_higher = medium_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.medium_ruined_holding.gold_high
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_high
							has_building_or_higher = large_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.large_ruined_holding.gold_high
				}
				# Generic treasure desc here
				# desc = lv_blocker_clearance.0001.desc.generic.gold_high
			}
			first_valid = {
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							has_building_or_higher = unexplored_caves_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.unexplored_caves.gold_low
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							lv_has_blocker_swamp = yes
						}
					}
					desc = lv_blocker_clearance.0001.desc.swamp.gold_low
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							has_building_or_higher = lv_floods_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.floods.gold_low
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							lv_has_blocker_noxious_gases = yes
						}
					}
					desc = lv_blocker_clearance.0001.desc.noxious_gases.gold_low
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							has_building_or_higher = stonemen_encampment_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.stonemen.gold_low
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							has_building_or_higher = bandit_camps_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.bandit_camps.gold_low
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							has_building_or_higher = small_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.small_ruined_holding.gold_low
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							has_building_or_higher = medium_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.medium_ruined_holding.gold_low
				}
				triggered_desc = {
					trigger = {
						scope:blocker_province = {
							var:blocker_outcome ?= flag:gold_low
							has_building_or_higher = large_ruined_holding_03
						}
					}
					desc = lv_blocker_clearance.0001.desc.large_ruined_holding.gold_low
				}
				# Generic treasure desc here
				# desc = lv_blocker_clearance.0001.desc.generic.gold_low
			}
		}
	}
	theme = realm

	right_portrait = {
		character = root
		scripted_animation = {
			triggered_animation = {
				trigger = { scope:blocker_province = { has_variable = blocker_outcome } }
				animation = { happiness }
			}
			animation = idle
		}
	}

	override_background = {
		trigger = { scope:blocker_province = { has_building_or_higher = unexplored_caves_03 } }
		reference = agot_dragonstone_cave
	}
	override_background = {
		trigger = { scope:blocker_province = { lv_has_blocker_swamp = yes } }
		reference = agot_taiga_bog
	}
	override_background = {
		trigger = { scope:blocker_province = { has_building_or_higher = lv_floods_03 } }
		reference = wilderness_wetlands
	}
	override_background = {
		trigger = { scope:blocker_province = { lv_has_blocker_noxious_gases = yes } }
		reference = battlefield
	}
	override_background = {
		trigger = { scope:blocker_province = { has_building_or_higher = stonemen_encampment_03 } }
		reference = battlefield
	}
	override_background = {
		trigger = { scope:blocker_province = { has_building_or_higher = bandit_camps_03 } }
		reference = ep2_hunt_poachers_camp
	}
	override_background = {
		trigger = { scope:blocker_province = { has_building_or_higher = small_ruined_holding_03 } }
		reference = courtyard
	}
	override_background = {
		trigger = { scope:blocker_province = { has_building_or_higher = medium_ruined_holding_03 } }
		reference = courtyard
	}
	override_background = {
		trigger = { scope:blocker_province = { has_building_or_higher = large_ruined_holding_03 } }
		reference = courtyard
	}

	artifact = { # To display the artifact in the event-window
		target = scope:newly_created_artifact
		position = lower_left_portrait
	}

	immediate = {
		if = {
			limit = { scope:blocker_province = { var:blocker_outcome ?= flag:dragon_egg } }
			agot_create_random_dragon_egg_artifact = { OWNER = root }
		}
		if = {
			limit = { scope:blocker_province = { var:blocker_outcome ?= flag:gold_high } }
			add_short_term_gold = 200
		}
		else_if = {
			limit = { scope:blocker_province = { var:blocker_outcome ?= flag:gold_low } }
			add_short_term_gold = 100
		}
	}

	option = { name = lv_blocker_clearance.0001.a }

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = unexplored_caves_03 }
				remove_building = unexplored_caves_03
			}
			if = {
				limit = { has_building = swamp_01_03 }
				remove_building = swamp_01_03
			}
			if = {
				limit = { has_building = swamp_02_03 }
				remove_building = swamp_02_03
			}
			if = {
				limit = { has_building = lv_floods_03 }
				remove_building = lv_floods_03
			}
			if = {
				limit = { has_building = noxious_gases_01_03 }
				remove_building = noxious_gases_01_03
			}
			if = {
				limit = { has_building = noxious_gases_02_03 }
				remove_building = noxious_gases_02_03
			}
			if = {
				limit = { has_building = stonemen_encampment_03 }
				remove_building = stonemen_encampment_03
			}
			if = {
				limit = { has_building = bandit_camps_03 }
				remove_building = bandit_camps_03
			}
			if = {
				limit = { has_building = small_ruined_holding_03 }
				remove_building = small_ruined_holding_03
			}
			if = {
				limit = { has_building = medium_ruined_holding_03 }
				remove_building = medium_ruined_holding_03
			}
			if = {
				limit = { has_building = large_ruined_holding_03 }
				remove_building = large_ruined_holding_03
			}
			hidden_effect = {
				lv_blocker_cleared_effect = yes
				remove_variable = blocker_outcome
			}
		}
	}
}

# Swamp: Challenge
lv_blocker_clearance.0011 = {
	type = character_event
	title = lv_blocker_clearance.0011.t
	desc = lv_blocker_clearance.0011.desc
	theme = realm

	right_portrait = {
		character = root
		animation = thinking
	}

	left_portrait = {
		character = scope:councillor
		animation = steward
	}

	override_background = agot_taiga_bog

	immediate = {
		if = {
			limit = { has_councillor_for_skill = stewardship }
			random_stewardship_councillor = { save_scope_as = councillor }
		}
	}

	# Ruler performs the skill check
	option = {
		name = lv_blocker_clearance.0011.a
		duel = {
			skill = stewardship
			value = high_skill_rating
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = 3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_success
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0014 } # Success
					50 = { trigger_event = lv_blocker_clearance.0015 } # Critical Success
				}
			}
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_failure
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0012 } # Failure
					50 = { trigger_event = lv_blocker_clearance.0013 } # Critical Failure
				}
			}
		}
	}

	# Councillor performs the skill check
	option = {
		trigger = { exists = scope:councillor }
		name = lv_blocker_clearance.0011.b
		scope:councillor = {
			duel = {
				skill = stewardship
				value = high_skill_rating
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_success
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0014 } # Success
							50 = { trigger_event = lv_blocker_clearance.0015 } # Critical Success
						}
					}
				}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_failure
					# Failure event
					# Revise
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0012 } # Failure
							50 = { trigger_event = lv_blocker_clearance.0013 } # Critical Failure
						}
					}
				}
			}
		}
	}
}

# Swamp: Failure
lv_blocker_clearance.0012 = {
	type = character_event
	title = lv_blocker_clearance.0012.t
	desc = lv_blocker_clearance.0012.desc
	theme = realm

	right_portrait = {
		character = root
		animation = worry
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0012.a
		add_prestige = medium_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = swamp_01_03 }
				remove_building = swamp_01_03
				add_building = swamp_01_01
			}
			if = {
				limit = { has_building = swamp_02_03 }
				remove_building = swamp_02_03
				add_building = swamp_02_01
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Swamp: Critical Failure (Hard Reset)
lv_blocker_clearance.0013 = {
	type = character_event
	title = lv_blocker_clearance.0013.t
	desc = lv_blocker_clearance.0013.desc
	theme = realm

	right_portrait = {
		character = root
		animation = stress
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0013.a
		add_prestige = major_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = swamp_01_03 }
				remove_building = swamp_01_03
				add_building = swamp_01_01
			}
			if = {
				limit = { has_building = swamp_02_03 }
				remove_building = swamp_02_03
				add_building = swamp_02_01
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Swamp: Success
lv_blocker_clearance.0014 = {
	type = character_event
	title = lv_blocker_clearance.0014.t
	desc = lv_blocker_clearance.0014.desc
	theme = realm

	right_portrait = {
		character = root
		animation = happiness
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0014.a
		add_prestige = medium_prestige_gain
	}

	after = { trigger_event = lv_blocker_clearance.0001 }
}

# Swamp: Critical Success
lv_blocker_clearance.0015 = {
	type = character_event
	title = lv_blocker_clearance.0015.t
	desc = lv_blocker_clearance.0015.desc
	theme = realm

	right_portrait = {
		character = root
		animation = ecstasy
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0015.a
		add_prestige = major_prestige_gain
	}

	after = {
		scope:blocker_province = {
			random_list = {
				1 = { # Find a dragon egg
					set_variable = {
						name = blocker_outcome
						value = flag:dragon_egg
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_high
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_low
					}
				}
			}
		}
		trigger_event = lv_blocker_clearance.0001
	}
}

# Floods: Challenge
lv_blocker_clearance.0021 = {
	type = character_event
	title = lv_blocker_clearance.0021.t
	desc = lv_blocker_clearance.0021.desc
	theme = realm

	right_portrait = {
		character = root
		animation = thinking
	}

	left_portrait = {
		character = scope:councillor
		animation = steward
	}

	override_background = wilderness_wetlands

	immediate = {
		if = {
			limit = { has_councillor_for_skill = stewardship }
			random_stewardship_councillor = { save_scope_as = councillor }
		}
	}

	# Ruler performs the skill check
	option = {
		name = lv_blocker_clearance.0021.a
		duel = {
			skill = stewardship
			value = high_skill_rating
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = 3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_success
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0024 } # Success
					50 = { trigger_event = lv_blocker_clearance.0025 } # Critical Success
				}
			}
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_failure
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0022 } # Failure
					50 = { trigger_event = lv_blocker_clearance.0023 } # Critical Failure
				}
			}
		}
	}

	# Councillor performs the skill check
	option = {
		trigger = { exists = scope:councillor }
		name = lv_blocker_clearance.0021.b
		scope:councillor = {
			duel = {
				skill = stewardship
				value = high_skill_rating
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_success
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0024 } # Success
							50 = { trigger_event = lv_blocker_clearance.0025 } # Critical Success
						}
					}
				}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_failure
					# Failure event
					# Revise
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0022 } # Failure
							50 = { trigger_event = lv_blocker_clearance.0023 } # Critical Failure
						}
					}
				}
			}
		}
	}
}

# Floods: Failure
lv_blocker_clearance.0022 = {
	type = character_event
	title = lv_blocker_clearance.0022.t
	desc = lv_blocker_clearance.0022.desc
	theme = realm

	right_portrait = {
		character = root
		animation = worry
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0022.a
		add_prestige = medium_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = lv_floods_03 }
				remove_building = lv_floods_03
				add_building = lv_floods_02
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Floods: Critical Failure (Hard Reset)
lv_blocker_clearance.0023 = {
	type = character_event
	title = lv_blocker_clearance.0023.t
	desc = lv_blocker_clearance.0023.desc
	theme = realm

	right_portrait = {
		character = root
		animation = stress
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0023.a
		add_prestige = major_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = lv_floods_03 }
				remove_building = lv_floods_03
				add_building = lv_floods_01
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Floods: Success
lv_blocker_clearance.0024 = {
	type = character_event
	title = lv_blocker_clearance.0024.t
	desc = lv_blocker_clearance.0024.desc
	theme = realm

	right_portrait = {
		character = root
		animation = happiness
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0024.a
		add_prestige = medium_prestige_gain
	}

	after = { trigger_event = lv_blocker_clearance.0001 }
}

# Floods: Critical Success
lv_blocker_clearance.0025 = {
	type = character_event
	title = lv_blocker_clearance.0025.t
	desc = lv_blocker_clearance.0025.desc
	theme = realm

	right_portrait = {
		character = root
		animation = ecstasy
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0025.a
		add_prestige = major_prestige_gain
	}

	after = {
		scope:blocker_province = {
			random_list = {
				1 = { # Find a dragon egg
					set_variable = {
						name = blocker_outcome
						value = flag:dragon_egg
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_high
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_low
					}
				}
			}
		}
		trigger_event = lv_blocker_clearance.0001
	}
}

# Noxious Gases: Challenge
lv_blocker_clearance.0031 = {
	type = character_event
	title = lv_blocker_clearance.0031.t
	desc = lv_blocker_clearance.0031.desc
	theme = realm

	right_portrait = {
		character = root
		animation = thinking
	}

	left_portrait = {
		character = scope:councillor
		animation = steward
	}

	override_background = battlefield

	immediate = {
		if = {
			limit = { has_councillor_for_skill = stewardship }
			random_stewardship_councillor = { save_scope_as = councillor }
		}
	}

	# Ruler performs the skill check
	option = {
		name = lv_blocker_clearance.0031.a
		save_scope_as = challenger
		duel = {
			skill = stewardship
			value = high_skill_rating
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = 3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_success
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0034 } # Success
					50 = { trigger_event = lv_blocker_clearance.0035 } # Critical Success
				}
			}
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_failure
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0032 } # Failure
					50 = { trigger_event = lv_blocker_clearance.0033 } # Critical Failure
				}
			}
		}
	}

	# Councillor performs the skill check
	option = {
		trigger = { exists = scope:councillor }
		name = lv_blocker_clearance.0031.b
		scope:councillor = {
			save_scope_as = challenger
			duel = {
				skill = stewardship
				value = high_skill_rating
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_success
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0034 } # Success
							50 = { trigger_event = lv_blocker_clearance.0035 } # Critical Success
						}
					}
				}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_failure
					# Failure event
					# Revise
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0032 } # Failure
							50 = { trigger_event = lv_blocker_clearance.0033 } # Critical Failure
						}
					}
				}
			}
		}
	}
}

# Noxious Gases: Failure
lv_blocker_clearance.0032 = {
	type = character_event
	title = lv_blocker_clearance.0032.t
	desc = {
		desc = lv_blocker_clearance.0032.desc
		triggered_desc = {
			trigger = {
				scope:challenger ?= {
					this = root
					has_trait = ill
				}
			}
			desc = lv_blocker_clearance.0032.desc.illness.self
		}
		triggered_desc = {
			trigger = {
				scope:challenger ?= {
					this = scope:councillor
					has_trait = ill
				}
			}
			desc = lv_blocker_clearance.0032.desc.illness.councillor
		}
	}
	theme = realm

	right_portrait = {
		character = root
		animation = worry
	}

	override_background = battlefield

	# Challenger has a chance of becoming ill
	immediate = {
		scope:challenger = {
			random = {
				chance = { # Add more modifiers
					value = 25
				}
				add_trait = ill
			}
		}
	}

	option = {
		name = lv_blocker_clearance.0032.a
		add_prestige = medium_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = noxious_gases_01_03 }
				remove_building = noxious_gases_01_03
				add_building = noxious_gases_01_02
			}
			if = {
				limit = { has_building = noxious_gases_02_03 }
				remove_building = noxious_gases_02_03
				add_building = noxious_gases_02_02
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Noxious Gases: Critical Failure (Hard Reset)
# Note to self: Set an immediate with a chance of starting a plague, then add additional triggered desc to main text body
lv_blocker_clearance.0033 = {
	type = character_event
	title = lv_blocker_clearance.0033.t
	desc = lv_blocker_clearance.0033.desc
	theme = realm

	right_portrait = {
		character = root
		animation = stress
	}

	override_background = battlefield

	# Chance of starting an outbreak
	#immediate = {
	#	create_epidemic_outbreak = {
	#		type = smallpox
	#		intensity = major
	#		save_scope_as = new_epidemic # optional
	#	}
	#}

	option = {
		name = lv_blocker_clearance.0033.a
		add_prestige = major_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = noxious_gases_01_03 }
				remove_building = noxious_gases_01_03
				add_building = noxious_gases_01_01
			}
			if = {
				limit = { has_building = noxious_gases_02_03 }
				remove_building = noxious_gases_02_03
				add_building = noxious_gases_02_01
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Noxious Gases: Success
lv_blocker_clearance.0034 = {
	type = character_event
	title = lv_blocker_clearance.0034.t
	desc = {
		desc = lv_blocker_clearance.0034.desc
		triggered_desc = {
			trigger = {
				scope:challenger ?= {
					this = root
					has_trait = ill
				}
			}
			desc = lv_blocker_clearance.0034.desc.illness.self
		}
		triggered_desc = {
			trigger = {
				scope:challenger ?= {
					this = scope:councillor
					has_trait = ill
				}
			}
			desc = lv_blocker_clearance.0034.desc.illness.councillor
		}
	}
	theme = realm

	right_portrait = {
		character = root
		animation = happiness
	}

	override_background = battlefield

	# Challenger has a chance of becoming ill
	immediate = {
		scope:challenger = {
			random = {
				chance = { # Add more modifiers
					value = 25
				}
				add_trait = ill
			}
		}
	}

	option = {
		name = lv_blocker_clearance.0034.a
		add_prestige = medium_prestige_gain
	}

	after = { trigger_event = lv_blocker_clearance.0001 }
}

# Noxious Gases: Critical Success
lv_blocker_clearance.0035 = {
	type = character_event
	title = lv_blocker_clearance.0035.t
	desc = lv_blocker_clearance.0035.desc
	theme = realm

	right_portrait = {
		character = root
		animation = ecstasy
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0035.a
		add_prestige = major_prestige_gain
	}

	after = {
		scope:blocker_province = {
			random_list = {
				1 = { # Find a dragon egg
					set_variable = {
						name = blocker_outcome
						value = flag:dragon_egg
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_high
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_low
					}
				}
			}
		}
		trigger_event = lv_blocker_clearance.0001
	}
}

# Stonemen: Challenge
lv_blocker_clearance.0041 = {
	type = character_event
	title = lv_blocker_clearance.0041.t
	desc = lv_blocker_clearance.0041.desc
	theme = realm

	right_portrait = {
		character = root
		animation = thinking
	}

	left_portrait = {
		character = scope:councillor
		animation = marshal
	}

	override_background = battlefield

	immediate = {
		if = {
			limit = { has_councillor_for_skill = martial }
			random_martial_councillor = { save_scope_as = councillor }
		}
	}

	# Ruler performs the skill check
	option = {
		name = lv_blocker_clearance.0011.a
		duel = {
			skill = martial
			value = high_skill_rating
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = 3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_success
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0044 } # Success
					50 = { trigger_event = lv_blocker_clearance.0045 } # Critical Success
				}
			}
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_failure
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0042 } # Failure
					50 = { trigger_event = lv_blocker_clearance.0043 } # Critical Failure
				}
			}
		}
	}

	# Councillor performs the skill check
	option = {
		trigger = { exists = scope:councillor }
		name = lv_blocker_clearance.0011.b
		scope:councillor = {
			duel = {
				skill = martial	
				value = high_skill_rating
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_success
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0044 } # Success
							50 = { trigger_event = lv_blocker_clearance.0045 } # Critical Success
						}
					}
				}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_failure
					# Failure event
					# Revise
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0042 } # Failure
							50 = { trigger_event = lv_blocker_clearance.0043 } # Critical Failure
						}
					}
				}
			}
		}
	}
}

# Stonemen Encampment: Failure
lv_blocker_clearance.0042 = {
	type = character_event
	title = lv_blocker_clearance.0042.t
	desc = lv_blocker_clearance.0042.desc
	theme = realm

	right_portrait = {
		character = root
		animation = worry
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0042.a
		add_prestige = medium_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = stonemen_encampment_03 }
				remove_building = stonemen_encampment_03
				add_building = stonemen_encampment_02
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Stonemen Encampment: Critical Failure (Hard Reset)
lv_blocker_clearance.0043 = {
	type = character_event
	title = lv_blocker_clearance.0043.t
	desc = lv_blocker_clearance.0043.desc
	theme = realm

	right_portrait = {
		character = root
		animation = stress
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0043.a
		add_prestige = major_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = stonemen_encampment_03 }
				remove_building = stonemen_encampment_03
				add_building = stonemen_encampment_01
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Stonemen Encampment: Success
lv_blocker_clearance.0044 = {
	type = character_event
	title = lv_blocker_clearance.0044.t
	desc = lv_blocker_clearance.0044.desc
	theme = realm

	right_portrait = {
		character = root
		animation = happiness
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0044.a
		add_prestige = medium_prestige_gain
	}

	after = { trigger_event = lv_blocker_clearance.0001 }
}

# Stonemen Encampment: Critical Success
lv_blocker_clearance.0045 = {
	type = character_event
	title = lv_blocker_clearance.0045.t
	desc = lv_blocker_clearance.0045.desc
	theme = realm

	right_portrait = {
		character = root
		animation = ecstasy
	}

	override_background = battlefield

	option = {
		name = lv_blocker_clearance.0045.a
		add_prestige = major_prestige_gain
	}

	after = {
		scope:blocker_province = {
			random_list = {
				1 = { # Find a dragon egg
					set_variable = {
						name = blocker_outcome
						value = flag:dragon_egg
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_high
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_low
					}
				}
			}
		}
		trigger_event = lv_blocker_clearance.0001
	}
}

# Bandit Camps: Challenge
lv_blocker_clearance.0051 = {
	type = character_event
	title = lv_blocker_clearance.0051.t
	desc = lv_blocker_clearance.0051.desc
	theme = realm

	right_portrait = {
		character = scope:martial_councillor
		animation = marshal
	}

	left_portrait = {
		character = scope:diplomacy_councillor
		animation = chancellor
	}

	override_background = ep2_hunt_poachers_camp

	immediate = {
		if = {
			limit = { has_councillor_for_skill = martial }
			random_martial_councillor = { save_scope_as = martial_councillor }
		}
		if = {
			limit = { has_councillor_for_skill = diplomacy }
			random_diplomacy_councillor = { save_scope_as = diplomacy_councillor }
		}
	}

	# Ruler performs the skill check
	option = {
		name = lv_blocker_clearance.0051.a
		save_scope_as = martial_chosen
		duel = {
			skill = martial
			value = high_skill_rating
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = 3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_success
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0054 } # Success
					50 = { trigger_event = lv_blocker_clearance.0055 } # Critical Success
				}
			}
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_failure
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0052 } # Failure
					50 = { trigger_event = lv_blocker_clearance.0053 } # Critical Failure
				}
			}
		}
	}

	# Councillor performs the skill check
	option = {
		trigger = { exists = scope:martial_councillor }
		name = lv_blocker_clearance.0051.b
		save_scope_as = martial_chosen
		scope:martial_councillor = {
			duel = {
				skill = martial
				value = high_skill_rating
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_success
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0054 } # Success
							50 = { trigger_event = lv_blocker_clearance.0055 } # Critical Success
						}
					}
				}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_failure
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0052 } # Failure
							50 = { trigger_event = lv_blocker_clearance.0053 } # Critical Failure
						}
					}
				}
			}
		}
	}

	# Ruler performs the skill check
	option = {
		name = lv_blocker_clearance.0051.c
		save_scope_as = diplomacy_chosen
		duel = {
			skill = diplomacy
			value = high_skill_rating
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = 3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_success
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0054 } # Success
					50 = { trigger_event = lv_blocker_clearance.0055 } # Critical Success
				}
			}
			10 = {
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
					min = 2
				}
				desc = reinforce_soldiers_skill_failure
				random_list = {
					50 = { trigger_event = lv_blocker_clearance.0052 } # Failure
					50 = { trigger_event = lv_blocker_clearance.0053 } # Critical Failure
				}
			}
		}
	}

	# Councillor performs the skill check
	option = {
		trigger = { exists = scope:diplomacy_councillor }
		name = lv_blocker_clearance.0051.d
		save_scope_as = diplomacy_chosen
		scope:diplomacy_councillor = {
			duel = {
				skill = diplomacy
				value = high_skill_rating
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_success
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0054 } # Success
							50 = { trigger_event = lv_blocker_clearance.0055 } # Critical Success
						}
					}
				}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -3.5
						min = 2
					}
					desc = reinforce_soldiers_skill_failure
					root = {
						random_list = {
							50 = { trigger_event = lv_blocker_clearance.0052 } # Failure
							50 = { trigger_event = lv_blocker_clearance.0053 } # Critical Failure
						}
					}
				}
			}
		}
	}
}

# Bandit Camp Encampment: Failure
lv_blocker_clearance.0052 = {
	type = character_event
	title = {
		triggered_desc = {
			trigger = { exists = scope:martial_chosen }
			desc = lv_blocker_clearance.0052.t.martial
		}
		triggered_desc = {
			trigger = { exists = scope:diplomacy_chosen }
			desc = lv_blocker_clearance.0052.t.diplomacy
		}
	}
	desc = {
		triggered_desc = {
			trigger = { exists = scope:martial_chosen }
			desc = lv_blocker_clearance.0052.desc.martial
		}
		triggered_desc = {
			trigger = { exists = scope:diplomacy_chosen }
			desc = lv_blocker_clearance.0052.desc.diplomacy
		}
	}
	theme = realm

	right_portrait = {
		character = root
		animation = worry
	}

	override_background = battlefield

	option = {
		name = {
			trigger = { exists = scope:martial_chosen }
			text = lv_blocker_clearance.0052.a.martial
		}
		name = {
			trigger = { exists = scope:diplomacy_chosen }
			text = lv_blocker_clearance.0052.a.diplomacy
		}
		add_prestige = medium_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = bandit_camps_03 }
				remove_building = bandit_camps_03
				add_building = bandit_camps_02
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Bandit Camp Encampment: Critical Failure (Hard Reset)
lv_blocker_clearance.0053 = {
	type = character_event
	title = {
		triggered_desc = {
			trigger = { exists = scope:martial_chosen }
			desc = lv_blocker_clearance.0053.t.martial
		}
		triggered_desc = {
			trigger = { exists = scope:diplomacy_chosen }
			desc = lv_blocker_clearance.0053.t.diplomacy
		}
	}
	desc = {
		triggered_desc = {
			trigger = { exists = scope:martial_chosen }
			desc = lv_blocker_clearance.0053.desc.martial
		}
		triggered_desc = {
			trigger = { exists = scope:diplomacy_chosen }
			desc = lv_blocker_clearance.0053.desc.diplomacy
		}
	}
	theme = realm

	right_portrait = {
		character = root
		animation = stress
	}

	override_background = battlefield

	option = {
		name = {
			trigger = { exists = scope:martial_chosen }
			text = lv_blocker_clearance.0053.a.martial
		}
		name = {
			trigger = { exists = scope:diplomacy_chosen }
			text = lv_blocker_clearance.0053.a.martial
		}
		add_prestige = major_prestige_loss
	}

	after = {
		scope:blocker_province = {
			if = {
				limit = { has_building = bandit_camps_03 }
				remove_building = bandit_camps_03
				add_building = bandit_camps_01
			}
			hidden_effect = { remove_variable = blocker_outcome }
		}
	}
}

# Bandit Camp Encampment: Success
lv_blocker_clearance.0054 = {
	type = character_event
	title = {
		triggered_desc = {
			trigger = { exists = scope:martial_chosen }
			desc = lv_blocker_clearance.0054.t.martial
		}
		triggered_desc = {
			trigger = { exists = scope:diplomacy_chosen }
			desc = lv_blocker_clearance.0054.t.diplomacy
		}
	}
	desc = {
		triggered_desc = {
			trigger = { exists = scope:martial_chosen }
			desc = lv_blocker_clearance.0054.desc.martial
		}
		triggered_desc = {
			trigger = { exists = scope:diplomacy_chosen }
			desc = lv_blocker_clearance.0054.desc.diplomacy
		}
	}
	theme = realm

	right_portrait = {
		character = root
		animation = happiness
	}

	override_background = battlefield

	option = {
		name = {
			trigger = { exists = scope:martial_chosen }
			text = lv_blocker_clearance.0054.a.martial
		}
		name = {
			trigger = { exists = scope:diplomacy_chosen }
			text = lv_blocker_clearance.0054.a.martial
		}
		add_prestige = medium_prestige_gain
	}

	after = { trigger_event = lv_blocker_clearance.0001 }
}

# Bandit Camp Encampment: Critical Success
lv_blocker_clearance.0055 = {
	type = character_event
	title = {
		triggered_desc = {
			trigger = { exists = scope:martial_chosen }
			desc = lv_blocker_clearance.0055.t.martial
		}
		triggered_desc = {
			trigger = { exists = scope:diplomacy_chosen }
			desc = lv_blocker_clearance.0055.t.diplomacy
		}
	}
	desc = {
		triggered_desc = {
			trigger = { exists = scope:martial_chosen }
			desc = lv_blocker_clearance.0055.desc.martial
		}
		triggered_desc = {
			trigger = { exists = scope:diplomacy_chosen }
			desc = lv_blocker_clearance.0055.desc.diplomacy
		}
	}
	theme = realm

	right_portrait = {
		character = root
		animation = ecstasy
	}

	override_background = battlefield

	option = {
		name = {
			trigger = { exists = scope:martial_chosen }
			text = lv_blocker_clearance.0055.a.martial
		}
		name = {
			trigger = { exists = scope:diplomacy_chosen }
			text = lv_blocker_clearance.0055.a.martial
		}
		add_prestige = major_prestige_gain
	}

	after = {
		scope:blocker_province = {
			random_list = {
				1 = { # Find a dragon egg
					set_variable = {
						name = blocker_outcome
						value = flag:dragon_egg
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_high
					}
				}
				1 = { # Find a cache of gold
					set_variable = {
						name = blocker_outcome
						value = flag:gold_low
					}
				}
			}
		}
		trigger_event = lv_blocker_clearance.0001
	}
}
